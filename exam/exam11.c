#include <stdio.h>
#include <stdlib.h>

int main()
{
    float a[10000];
    int b[3000];
    int i;
    int n;
    scanf("%d", &n);
    for(i=0;i<n*3;i++){
        scanf("%d", a[i]);
    }
    for
    return 0;
}